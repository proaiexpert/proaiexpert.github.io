name: ProAI Semantic R4 Owner Preview
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled during live-owner-review-only window; no video/ffmpeg.'
