name: ProAI Cube R4.4.3 Bounded Owner Review
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled during live-owner-review-only window; no video/ffmpeg.'
