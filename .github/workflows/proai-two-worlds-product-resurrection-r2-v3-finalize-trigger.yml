name: Trigger ProAI Two Worlds Product Resurrection R2 V3 Finalize V3

on:
  push:
    branches:
      - agent/proai-two-worlds-product-resurrection-r2
    paths:
      - .github/workflows/proai-two-worlds-product-resurrection-r2-v3-finalize-trigger.yml

permissions:
  actions: write
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch race-free final committed-evidence verifier V3
        shell: bash
        run: |
          set -euo pipefail
          curl -fsS -X POST \
            -H "Authorization: Bearer ${{ github.token }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/proai-two-worlds-product-resurrection-r2-v3-finalize-v3.yml/dispatches \
            -d '{"ref":"agent/proai-two-worlds-product-resurrection-r2"}'
