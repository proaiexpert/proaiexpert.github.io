(()=>{'use strict';const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const targets=[...document.querySelectorAll('[data-r2-reveal],[data-r2-sequence]')];const activate=el=>{if(el.classList.contains('is-r2-live'))return;el.classList.add('is-r2-live')};if(reduce||!('IntersectionObserver'in window)){targets.forEach(activate);return}const io=new IntersectionObserver((entries,observer)=>{for(const entry of entries){if(!entry.isIntersecting)continue;activate(entry.target);observer.unobserve(entry.target)}},{threshold:.18,rootMargin:'0px 0px -8% 0px'});targets.forEach(el=>io.observe(el));})();
