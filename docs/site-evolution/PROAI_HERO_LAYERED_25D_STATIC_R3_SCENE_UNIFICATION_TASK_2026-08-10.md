# PROAI EXPERT HERO — LAYERED 2.5D STATIC R3 — SCENE UNIFICATION / FOUNDATION LOCK

Repository: `proaiexpert/proaiexpert.github.io`

Working branch: `agent/hero-layered-25d-static-r1`

Status at task creation: `READY FOR EXECUTION`

---

## 0. EXECUTION AUTHORITY

This file is the **only current execution authority** for the next Hero pass on this branch.

This is a continuation of the existing Layered 2.5D work. Do not restart concept exploration, benchmark work, copy strategy, C-shape design, or raster-retouch experiments.

Read first:

- `docs/site-evolution/PROAI_HERO_OWNER_WORKFLOW_LOCK_2026-08-10.md`
- `docs/site-evolution/PROAI_HOMEPAGE_HERO_MASTER_RECOVERY_PACK_2026-08-10.md`
- `docs/site-evolution/PROAI_HOMEPAGE_HERO_ASSET_INDEX_2026-08-10.md`
- `docs/site-evolution/PROAI_EXPERT_HERO_RECOVERY_LOCK_2026-08-09.md`
- `docs/site-evolution/PROAI_HERO_LAYERED_25D_STATIC_R1_STATUS.md`
- `docs/site-evolution/PROAI_HERO_LAYERED_25D_STATIC_R2_STATUS.md`

The owner is NEVER a context/file transport layer. Recover every required source, PNG, registered layer, SHA and prior candidate yourself from GitHub / indexed recovery sources. Do not ask the owner to upload, attach, download, move, resend, copy or relay anything manually.

---

## 1. CURRENT FOUNDATION / EXACT SOURCE

Use the exact R2 candidate as the current visual foundation:

`docs/site-evolution/review-artifacts/PROAI_HERO_LAYERED_25D_STATIC_R2.png`

R2 candidate commit:

`b2e94c33965e12e0085153fb48ebc6a6e5d0f91e`

R2 SHA-256:

`fd049642b4b27b373f441621164ef3d1e7d2adf59f07bfe6061ca7b2e2a33ea2`

R2 is strictly better than R1 and must be the continuation base.

Preserve these R2 fixes:

- floating / broken geometry fragment removed;
- `RU` language control has the subtle chevron;
- `View Work →` has intentional continuous underline treatment;
- important microcopy / geo-language trust line have improved contrast;
- approved C-shape, perspective and layered 2.5D architecture remain intact;
- Core remains approximately `-4.5%` from the recovered registration;
- calm rail remains `01 TRUST / 02 INQUIRY / 03 RESPONSE / 04 RESULT`;
- output-to-row geometry and foreground occlusion logic remain intact.

Do not regress any of the above.

---

## 2. OWNER VERDICT ON R2 — ROOT PROBLEM STILL PRESENT

R2 fixed several local issues, but the Hero still reads as two different surfaces joined in the middle.

The visible seam is **not primarily a blur problem**.

The root cause is that the left copy field and the right Core scene still have perceptibly different base background colors / tonal families:

- left side retains a greenish / cyan-black cast from the historical Hero field;
- right Core plate reads closer to clean near-black;
- the transition therefore creates a vertical dark cutoff / black stripe / split even after atmospheric blending.

This is unacceptable for the premium target.

The next pass must eliminate the two-surface architecture itself.

---

## 3. PRIMARY OBJECTIVE

Produce one final static foundation candidate in which the full Hero reads as **one continuous near-black cinematic surface**.

Target perception:

> ONE HERO BACKGROUND + ONE PHYSICAL CORE SCENE + LOCALIZED SOFT ATMOSPHERE

Not:

> GREENISH LEFT PANEL + BLACK RIGHT IMAGE + BLURRED SEAM

This is a scene-unification pass, not a new design.

---

## 4. CRITICAL METHOD CHANGE — GLOBAL BACKGROUND UNLOCK

The previous requirement to preserve the complete left side pixel-identically is now **partially superseded**.

### Preserve exactly / near-exactly

Preserve the existing geometry and visual hierarchy of:

- Header layout;
- logo and navigation placement;
- H1 position, line breaks, font family, size, weight and tracking;
- support-copy position and typography;
- primary CTA position / dimensions / typography;
- secondary CTA position / treatment;
- microcopy positions;
- trust-line position;
- left/right spatial composition;
- C-shape geometry / perspective / registration;
- rail positions and labels.

### Explicitly unlocked

The **background pixels underneath the left copy field are no longer pixel-locked**.

They may and should be replaced so that the entire Hero body uses one common near-black base.

Do not preserve the old greenish background simply to satisfy an obsolete pixel-lock QA gate.

If necessary, reconstruct / render the left text and controls from the repository-backed HTML/CSS / canonical header and Hero sources so the typography and positions remain exact while the underlying background becomes unified.

Do not use fragile luminance-key extraction of the text from the old screenshot if a repository-backed DOM/CSS source is available.

---

## 5. P0 — ONE GLOBAL NEAR-BLACK BASE

Use one continuous base surface across the full Hero content area below the Header.

Preferred base family:

`#06080B` / equivalent existing ProAI near-black token.

The exact final RGB may be tuned slightly for integration, but there must be **one base field**, not separate left/right fields.

Requirements:

- no rectangular right-field background;
- no independent right-panel fill;
- no `x≈695` or equivalent hard background cutoff;
- no black vertical stripe between columns;
- no visible green-to-black panel transition;
- no blur patch used to hide a structural mismatch;
- no visible edge where an image plate starts or ends.

The right-side Core and its environment must sit on transparent / composited registered layers over this shared base wherever practical.

---

## 6. P0 — LOCAL ATMOSPHERE, NOT A SECOND BACKGROUND

Cyan / green / blue-steel atmosphere is allowed only as **localized light and volumetric response around the Core**.

It must be additive / transparent / spatially feathered over the common near-black base.

Use low-frequency, wide falloff fields rather than rectangular fills.

Desired behavior:

- strongest around / behind the internal chamber and lower Core;
- progressively weaker toward the center divide;
- may softly extend behind the center area to visually connect the composition;
- must reach zero / near-zero imperceptibly rather than terminate on a vertical line;
- must not tint the entire left copy panel green again;
- must not reduce H1 / body-copy readability.

Think **light spill in one room**, not **a second colored panel**.

---

## 7. P1 — STATIC COMPOSITION BRIDGE BETWEEN COPY AND CORE

R2 still reads as two parallel halves with insufficient physical relationship.

Create a restrained static bridge without adding a literal SVG beam across the text.

Allowed bridge cues:

- extremely soft cyan / graphite light spill extending from the Core toward the central whitespace;
- subtle shared floor / atmosphere response crossing the former boundary;
- faint value / reflection continuity that makes both halves occupy the same environment;
- very restrained left-to-right directional illumination logic.

Forbidden:

- bright line from CTA/text to Core;
- decorative cable;
- neon beam;
- visible rectangular gradient plate;
- fog wall;
- heavy bloom.

The stronger semantic bridge may later be expressed by motion, but the static must already feel like one scene.

---

## 8. P1 — PRESERVE / CLEAN R2 MATERIAL AND CHAMBER

Do not rebuild the object again.

Keep R2 material/chamber improvements and only correct them if scene unification requires it.

Preserve:

- clean graphite/steel integrity;
- front-shell occlusion;
- chamber recess / mid / deep hierarchy;
- restrained cyan depth;
- subtle warm counter-reflection;
- contact/floor grounding;
- non-cyberpunk restraint.

No additional effect escalation in this pass.

This pass is about making the foundation coherent, not making it louder.

---

## 9. P2 — MICROCOPY READABILITY

Preserve the R2 contrast improvements.

The line:

`Briefly describe the challenge. We’ll recommend where to start.`

may be increased **very slightly** in size / line-height if needed for desktop readability, provided this does not disturb the established layout hierarchy.

Guideline:

- maximum approximately `+1 px` from current rendered size;
- no new copy;
- no extra visual box / badge around it.

The trust line:

`Washington-based · Working across the U.S. · EN / RU / UA`

should remain restrained but clearly readable.

Do not promote either line to headline-level emphasis.

---

## 10. 01–04 RAIL — DO NOT ADD EXPLANATORY COPY

Do **not** add descriptions beneath:

- TRUST
- INQUIRY
- RESPONSE
- RESULT

The rail is intentionally a high-level journey cue in the static Hero.

Adding explanatory copy now would overload the right side and weaken the premium restraint.

The later motion phase can make the semantic progression clearer through controlled sequential signal behavior rather than more text.

Keep the existing four-row labels and alignment.

---

## 11. REQUIRED VISUAL QA — SEAM GATE

Do not rely only on automated geometry checks.

Before saving the owner candidate, inspect the full `1440 × 900` Hero at normal scale and enlarged crops around the former left/right boundary.

The candidate fails if any of the following are visible:

- vertical black stripe;
- green panel ending at the center;
- rectangular image edge;
- obvious right-field start boundary;
- different background blacks that read as two panels;
- blur-smear used to disguise the boundary;
- halo or matte edge around the C-shape scene;
- regression of the removed floating fragment.

The background must remain visually continuous even when brightness is temporarily raised for QA inspection.

If useful, perform a diagnostic exposure/gamma lift on a temporary QA copy to reveal hidden seams. Do not ship the diagnostic treatment.

---

## 12. REQUIRED OUTPUT

Produce exactly one owner-review static candidate:

`docs/site-evolution/review-artifacts/PROAI_HERO_LAYERED_25D_STATIC_R3.png`

Dimensions:

`1440 × 900`

Create status:

`docs/site-evolution/PROAI_HERO_LAYERED_25D_STATIC_R3_STATUS.md`

Status must remain:

`PENDING OWNER REVIEW`

until explicit owner approval.

Status must record:

- exact R2 commit and SHA used;
- exact global background strategy;
- confirmation that left background pixel-lock was intentionally released while typography/layout geometry remained locked;
- how the former seam / panel split was eliminated;
- how local Core atmosphere is composited over the common base;
- whether microcopy size changed and by how much;
- candidate dimensions and SHA-256;
- production safety confirmation.

After saving R3 and status, stop.

---

## 13. FUTURE PREMIUM EFFECTS / MOTION — RESERVED AFTER R3 APPROVAL

R3 is intended to become the static foundation for the next owner-approved enhancement phase.

After explicit R3 approval, the existing layered architecture may be used for a separate premium effects/motion phase including, where visually justified:

- richer cyan / blue-steel tonal variation;
- very restrained champagne / amber counter-reflections;
- depth-based parallax;
- subtle object tilt;
- internal light propagation;
- reflection lag;
- restrained glass / Fresnel response;
- microscopic optical dispersion only on appropriate glass/high-contrast boundaries;
- sparse internal depth particles;
- chamber breathing / energy modulation;
- sequential output signal impulses.

Do not implement these behaviors during R3.

The static foundation must be approved first.

---

## 14. SAFETY / FORBIDDEN

Do not:

- change the Hero concept;
- redesign or regenerate the C-shape;
- restart benchmark or copy strategy work;
- create alternate owner candidates;
- restore the old raster-retouch method;
- change production `/index.html` or `/ru/index.html`;
- merge;
- deploy;
- open a production PR;
- start motion;
- touch Financial Stream.

Final state: **one R3 static candidate, one R3 status, `PENDING OWNER REVIEW`, then stop.**