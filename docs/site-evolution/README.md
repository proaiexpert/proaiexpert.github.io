# PROAI EXPERT — Site Evolution Archive

This directory contains historical implementation records, review artifacts, experiments, screenshots, logs and superseded component states.

## Current authority

The sole current homepage product authority is:

`/PRODUCTION-AUTHORITY.md`

`main` is the sole current product branch.

Files under `docs/site-evolution/`, historical commits, review copies, donor branches and checkpoint branches are archival evidence only. They must not be used to select current product design, current proof metrics, current component status, current rollback authority or current release wiring.

If historical documentation conflicts with `main` or `/PRODUCTION-AUTHORITY.md`, stop and treat the historical record as stale.
