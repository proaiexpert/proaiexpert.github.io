# PROAI LOGO R3 — LIVE SITE LOGO REFERENCE

Verified against the live production site `https://proai-expert.com/` and production baseline `c945084e1952c05c686494091f7dbca0f7acdf08` before correcting the R3 review composition.

## Actual current production logo

Production Header source:
- `_includes/header-system/header.html`
- `assets/css/header-system-v1.css`

Actual lockup structure:
- left: small rotating CSS wireframe cube;
- right: one-line wordmark `PROAI EXPERT`;
- `PROAI`: white;
- `EXPERT`: canonical cyan `#5de2ff`;
- family: `Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`;
- weight: `900`;
- Header size: `20px` desktop;
- letter spacing: `-1px`;
- casing: uppercase;
- mark-to-wordmark gap: `12px`.

## R3 correction

The wordmark composition is not redesigned. R3 preserves the production wordmark language: single line, uppercase, heavy compact Inter hierarchy, white `PROAI` + cyan `EXPERT`.

The only identity-object substitution under review is:

`legacy CSS wireframe cube` → `existing canonical premium ProAI 3D Cube`

The canonical 3D Cube remains the exact frozen R4.1 physical object / runtime already documented in `PROVENANCE.json`. R3 does not invent new mark geometry.

The legacy production cyan text glow is not amplified in the premium large-form R3 review; the cyan color hierarchy is preserved while glow is removed from the review wordmark to satisfy the no-neon / no-glowing-letter quality constraint.

No production Header file is changed by this review.
