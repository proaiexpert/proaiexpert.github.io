# ProAI Expert Homepage V2 — Concept A Selected Specification

**Status:** corrected selected-concept specification candidate for focused review  
**Version:** V1.1  
**Date:** 2026-08-05  
**Repository:** `proaiexpert/proaiexpert.github.io`  
**Branch:** `agent/homepage-v2-concept-a-spec`  
**Selected direction:** Concept A — Precision Grid  
**Owner selection:** confirmed  
**Original independent-review verdict:** `TARGETED CORRECTION`  
**Original review commit:** `78abab43e2667d9ad4aa063f66cb5c19dc5cd462`  
**Production authorization:** none

---

# 1. Purpose

This document converts the accepted full-page visual direction **Concept A — Precision Grid** into a deterministic visual-system specification and future implementation contract for Homepage V2.

The specification is intended to prevent three classes of drift:

1. returning to excessive sci-fi spectacle;
2. becoming a generic SaaS/dashboard template;
3. weakening proof, localization, accessibility, or commercial hierarchy while reproducing the visual concept.

This document does not authorize production implementation. It defines what a later Builder must preserve after independent review and explicit implementation authorization.

The governing rule is:

> **Signal, not spectacle.**

Technical identity must come from precision, hierarchy, evidence, and connected-system logic. It must not depend on constant animation, oversized glow, 3D objects, glassmorphism, or decorative AI imagery.

## 1.1 V1.1 targeted correction scope

V1.1 resolves only the three deterministic defects identified by the independent selected-specification review:

1. section-surface ownership is assigned for all ten Homepage blocks;
2. subtle structural rules are separated from interactive-control and focus boundaries;
3. essential proof statuses use a mandatory `12px` mobile minimum.

These corrections do not redesign Concept A, change accepted copy, alter the ten-block order, modify proof taxonomy, change either Concept A SVG, or authorize implementation planning or production work.

---

# 2. Source authority and locked inputs

## 2.1 Accepted source chain

The selected specification inherits authority from:

1. Homepage V2 Strategy;
2. accepted Content Architecture V1.1;
3. accepted four-map low-fidelity system;
4. accepted full-page visual-concept review;
5. owner selection of Concept A.

Key accepted references:

```text
Accepted Content Architecture blob:
cdfe0188cdf5d886066f6effdceaf9e8bddd704b

Accepted low-fidelity final-review commit:
5702a4a0e0bd790000a5536ba48f6584f20d0f07

Accepted visual-review commit:
2fd5be4d3b5097cc2683424f88a185f7b76ec965

Concept A desktop SVG blob:
ede0c12db438e3985a36f8f447383dfd0e4eadcc

Concept A mobile SVG blob:
c8f5a69944c44046d6684b0a685f4a7b61d33407
```

## 2.2 Locked commercial order

The Homepage must preserve exactly this order:

1. Hero;
2. Connected Business Journey;
3. Two Core Directions;
4. Financial Stream flagship proof;
5. Ways to Start;
6. Controlled Delivery;
7. Founder accountability;
8. Selected Work;
9. Insights;
10. Final Private Review.

No selected-concept decision authorizes reordering, merging, deleting, or inserting a new top-level Homepage block.

## 2.3 Locked system boundaries

The following remain outside this specification and must not be redesigned incidentally:

- global Header system;
- global Footer system;
- Contact field contract and safe query allowlist;
- EN root and RU `/ru/` route architecture;
- canonical, reciprocal hreflang, and x-default behavior;
- project evidence taxonomy;
- case-study routes;
- current production deployment workflow.

The Homepage visual system must integrate with the existing Header and Footer. It must not create a competing local header or footer.

---

# 3. Selected visual identity

## 3.1 Positioning

Concept A represents:

> **Technical authority with disciplined premium restraint.**

The system must communicate:

- advanced AI and automation capability;
- controlled implementation rather than experimentation;
- premium international standards;
- trustworthiness for U.S. service-business owners;
- evidence-led work;
- direct founder accountability;
- precision without coldness;
- innovation without startup or crypto styling.

## 3.2 Visual character

The selected system uses:

- dark graphite rather than pure decorative black;
- strong white typography;
- one controlled cyan signal;
- precise section labels and numbering;
- restrained borders and technical grid atmosphere;
- moderate corner radii;
- large proof areas;
- linear, readable page rhythm;
- clear distinction between content, evidence, disclosure, and action.

## 3.3 Non-selected characteristics

The following are not part of Concept A and must not be imported during implementation:

- Concept B warm pearl page background as the main system;
- Concept C mint/violet product-startup palette;
- large capsule cards across all sections;
- continuous ambient glow;
- glassmorphism as a component language;
- crypto-style gradients;
- dashboard-heavy layouts;
- floating 3D AI objects;
- animated network scenes that compete with reading;
- generic consulting-template minimalism that removes the technical identity.

A limited lesson from Concept B is permitted: editorial spacing, proof clarity, and calm hierarchy. This must be expressed inside Concept A tokens rather than by merging the two concepts.

---

# 4. Design-token contract

The token values below are selected-direction values. Later implementation may adjust values only through a separately reviewed targeted correction when browser testing proves a measurable readability or contrast issue.

## 4.1 Color tokens

```text
page background                    #06080B
primary section surface            #0E1319
raised/card surface                #131B23
strong inner field                 #06080B
structural border/rule             #24313D
interactive control boundary       #66788C
primary focus signal               #66E3FF
primary text                        #F5F8FB
muted text                          #91A2B3
primary cyan signal                 #66E3FF
deep blue support                   #0A8DFF
```

### Usage rules

- `#66E3FF` is the dominant accent.
- `#0A8DFF` may support a restrained gradient or state, but must not compete with the primary accent.
- Accent must not be used for every heading, every border, and every icon.
- Primary body text must not use cyan.
- Muted text must remain readable against every assigned surface.
- Project evidence classes must not depend on color alone.
- Success, warning, or error colors are not Homepage brand decoration and must be introduced only for actual semantic states.
- `#24313D` is a subtle structural token for section rules, decorative dividers, non-essential card outlines, and atmospheric geometry. It must not be the sole visible boundary of an interactive control or the sole focus indicator.
- `#66788C` is the default interactive control-boundary token on the three specified dark surfaces. Its contrast must remain at least `3:1` against the actual adjacent surface after implementation.
- `#66E3FF` is the primary focus signal on dark surfaces. A focus treatment must remain distinguishable from both the component and its adjacent background.

## 4.2 Surface hierarchy

Use no more than three normal dark-surface levels on one viewport:

1. page background;
2. section/card surface;
3. raised proof or system field.

A fourth visual depth level requires a real semantic reason, such as an embedded screenshot frame, not decoration.

### 4.2.1 Locked section-surface ownership

The full-page surface rhythm is semantic and mandatory. Responsive recomposition may change layout, but it must not change the base surface assigned to a block.

| Block | Required base surface | Permitted internal depth |
|---|---|---|
| 1. Hero | `page background #06080B` plus restrained Hero atmosphere | supplementary tableau may use `primary section surface` or `raised/card surface` |
| 2. Connected Business Journey | `primary section surface #0E1319` | friction fields may use `raised/card surface`; steps may return to `page background` |
| 3. Two Core Directions | `page background #06080B` | direction fields may use `primary section surface` |
| 4. Financial Stream | `primary section surface #0E1319` | flagship proof and evidence fields may use `raised/card surface`; screenshot inner field may use `strong inner field` |
| 5. Ways to Start | `page background #06080B` | situation rows may use `primary section surface` |
| 6. Controlled Delivery | `primary section surface #0E1319` | stage fields may use `raised/card surface` |
| 7. Founder accountability | `page background #06080B` | portrait and accountability fields may use `primary section surface` or one restrained raised field |
| 8. Selected Work | `primary section surface #0E1319` | project fields may use `raised/card surface`; image inner fields may use `strong inner field` |
| 9. Insights | `page background #06080B` | article fields may use `primary section surface` |
| 10. Final Private Review | `primary section surface #0E1319` | CTA and bounded expectation copy may use normal control tokens; no additional full-width surface band |
| Locked Footer | owned by the existing global Footer system | this specification does not restyle it |

Rules:

- The alternating block rhythm must remain visible as a complete-page system on desktop and mobile.
- A Builder must not place all ten blocks on one uninterrupted background.
- A Builder must not invent a different alternation order.
- `raised/card surface #131B23` is an internal semantic depth, not a replacement base background for an entire ordinary block.
- Financial Stream remains the strongest proof through scale, composition, evidence density, and screenshot prominence; surface ownership alone must not flatten it into an ordinary card section.
- Grid and Hero glow may fade across surfaces, but they do not change surface ownership.
- Forced-colors mode may replace the palette while preserving grouping, boundaries, and reading order.

## 4.3 Borders, control boundaries, and focus roles

### Structural rule role

- Default decorative or structural rule: `1px` using `#24313D`.
- This role covers section dividers, atmospheric grid geometry, and non-essential card outlines.
- Low contrast is intentional only when the rule is decorative or when the component remains fully identifiable without it.
- Structural rules must never carry essential status, selection, control, or focus meaning by themselves.

### Interactive control-boundary role

- A secondary button, input-like control, toggle, or other interactive element that depends on an outline for identification must use `#66788C` or another separately verified token with at least `3:1` contrast against its actual adjacent surface.
- The boundary must remain distinguishable in default, hover, active, and disabled states where those states exist.
- Cyan may identify an active or selected state, but status or selection must not depend on color alone.
- A filled primary cyan button may use its fill silhouette as the control boundary when the fill has sufficient non-text contrast against the adjacent surface.
- `#24313D` must not be substituted for the interactive control boundary merely to make controls visually quieter.

### Focus-visible role

- Focus is not a decorative border state.
- On dark surfaces, use a minimum `2px` visible outline in `#66E3FF` with sufficient separation or offset from the component boundary.
- On cyan-filled controls, use a light `#F5F8FB` outer outline with a dark separation gap, or an equivalently verified treatment that remains visible against both the cyan control and surrounding surface.
- Focus must not be represented only by changing the border from `#24313D` to another low-contrast color.
- Focus treatment must meet non-text contrast requirements against the adjacent colors and remain visible at `200%` zoom.
- In forced-colors mode, native system focus colors and outlines take precedence; do not suppress them.

### General border discipline

- Strong active or conclusion border: maximum `1.5px` in the cyan signal unless the focus contract requires `2px`.
- Dividers should be thin and structural.
- Avoid multiple nested borders around every component.
- Decorative border subtlety must not reduce control discoverability.

## 4.4 Corner radii

```text
standard card, desktop       12px
standard card, mobile        10–12px
small internal field         6–8px
primary/secondary button     10–12px
image frame                   8–12px
```

Rules:

- Buttons are not fully pill-shaped.
- Large `24px+` product-card radii are prohibited as the default system.
- Radius must not be used to imply pricing plans or selectable SaaS modules.

## 4.5 Shadows and depth

- Default cards should rely on surface and border, not box shadow.
- If a proof screenshot needs separation, use one restrained low-opacity shadow.
- Neon outer glow is prohibited on normal text and cards.
- Cyan glow may appear only as a subtle atmospheric Hero field or rare focus signal.

## 4.6 Grid atmosphere

The technical grid is atmospheric, not a wallpaper pattern that dominates the entire page.

Requirements:

- line weight: approximately 1px;
- production opacity target: approximately `0.10–0.20`, subject to contrast testing;
- grid spacing may be approximately 40–64px on desktop and wider/less visible on mobile;
- no animated grid;
- no grid behind dense body text when it reduces readability;
- grid may fade across section boundaries;
- proof screenshots must not be obscured by the grid.

## 4.7 Hero atmosphere

A restrained cyan/deep-blue field may appear in the Hero.

- maximum practical visual intensity should remain below the text and primary CTA;
- the field must fade rather than create a bright neon corner;
- no rotating rings, particles, or constantly moving blobs are required;
- mobile may simplify or remove the atmospheric field while preserving identity through typography and accent.

---

# 5. Typography contract

## 5.1 Family

The selected concept uses a modern grotesk/sans direction represented by `Inter` in the concept artifacts.

Future implementation must:

- first inspect the repository's existing font strategy;
- avoid introducing an unreviewed font dependency;
- use a robust system fallback;
- preserve Cyrillic quality;
- avoid a font that looks gaming, sci-fi, crypto, or condensed-interface-only.

Candidate stack:

```text
Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
"Segoe UI", Arial, sans-serif
```

This stack is a specification candidate, not authorization to add font files or external font requests.

## 5.2 Typography scale

### Display / Hero H1

```text
desktop range       64–76px
mobile 390 range    40–46px
mobile 320 range    36–42px
font weight         780–850
line height         0.98–1.12 desktop; 1.08–1.18 mobile
letter spacing      negative, restrained; never causing Cyrillic collisions
```

The RU H1 must remain complete and naturally wrapped. It must not be shortened to make the visual concept easier.

### Section H2

```text
desktop             30–38px
mobile              24–30px
weight              700–800
line height         1.12–1.25
```

### Card heading / H3

```text
desktop             20–26px
mobile              20–25px
weight              650–780
line height         1.2–1.3
```

### Body

```text
desktop             16–18px
mobile              15–17px
line height         1.5–1.65
maximum text width  approximately 62–72 characters where practical
```

### Section numbers and non-essential compact metadata

```text
minimum desktop     11px
minimum mobile      11px
line height         1.35–1.5
letter spacing      maximum approximately 0.10em for long Cyrillic text
```

The `11px` mobile minimum is permitted only for short section numbers and non-essential compact metadata labels. It is not permitted for proof classification, project status, disclosure text, control labels, or essential action text.

### Proof statuses and essential evidence classifications

```text
minimum desktop     11px
minimum mobile      12px mandatory
line height         1.4–1.55
letter spacing      maximum approximately 0.08em for long Cyrillic status text
```

The exact Financial Stream, Alina Horb, and Local Repair Pro statuses are essential proof classifications. They must never fall below `12px` on mobile, including `390px`, `360px`, `320px`, and short-landscape compact modes. Long Cyrillic status text may use title case, reduced tracking, and additional natural lines, but must not use smaller type, break inside words, clip, or hide behind interaction.

### Disclosures and evidence boundaries

```text
minimum mobile      14px
minimum desktop     13px
line height         minimum 1.5
```

The small type in the SVG is conceptual. Production must use the minimum readable values above.

## 5.3 Uppercase use

Uppercase is permitted for:

- section numbers;
- short metadata labels;
- concise evidence status when the required minimum size and readable tracking are preserved;
- compact actions.

Uppercase is not permitted for:

- paragraphs;
- long disclosures;
- multi-sentence supporting copy;
- long RU text where letter spacing harms readability.

## 5.4 EN/RU resilience

- EN and RU may use different natural line breaks.
- Matching visual hierarchy is required; matching exact line count is not.
- RU text may require more vertical space.
- No fixed-height text container may clip localized copy.
- CTA width must accommodate localized labels.
- Status and disclosure containers must expand in normal flow.
- Essential mobile proof statuses must retain the mandatory `12px` minimum while expanding to additional lines where required.

---

# 6. Layout and spacing contract

## 6.1 Desktop container

At a 1440px viewport, the concept uses approximately:

```text
outer page margin       72px
maximum content width   1296px
primary grid            12 columns
standard gap            24–32px
```

The later implementation may use a fluid container but must preserve equivalent visual density and alignment.

## 6.2 Mobile container

```text
390px viewport gutter   20px
320px viewport gutter   16px minimum; 20px preferred when content permits
mobile grid             4 columns or normal-flow single column
mobile card gap         16–20px
```

No mobile content may rely on horizontal poster scrolling.

## 6.3 Vertical rhythm

Recommended target ranges:

```text
major section padding, desktop     72–104px
major section padding, mobile      48–72px
heading-to-content gap             24–40px
card internal padding, desktop     24–32px
card internal padding, mobile      16–22px
```

The page must feel deliberate and premium, not compressed. It must also avoid oversized empty sections that make the full journey feel artificially long.

## 6.4 Responsive checkpoints

The selected specification must prove behavior at minimum at:

1. `1440px` desktop;
2. `390px` portrait mobile;
3. `320px` narrow portrait mobile;
4. approximately `844 × 390` short landscape.

Additional fluid checks are required later around:

- 1280px;
- 1024px;
- 900px;
- 768px;
- 430px;
- 360px.

These checks do not create new content variants. They validate one responsive system.

---

# 7. Shared component grammar

## 7.1 Section header

Every major block may use:

- two-digit section number;
- concise section label;
- one thin horizontal rule;
- consistent baseline alignment.

The section-number system supports orientation. It must not become decorative noise.

## 7.2 Cards

Cards must communicate a real unit:

- direction;
- journey step;
- proof object;
- start situation;
- process stage;
- project;
- article.

Avoid cards around text that does not need a container.

## 7.3 Buttons

Primary button:

- cyan fill;
- dark text;
- clear focus state;
- moderate radius;
- no glow required.

Secondary button:

- transparent or surface fill;
- visible `interactive control boundary`, not the subtle structural-rule token;
- primary text;
- equal minimum touch target.

Minimum interactive target:

```text
44 × 44 CSS px
```

## 7.4 Links

Text links may use cyan and an arrow, but must remain identifiable without color alone through underline, offset rule, icon, or consistent placement.

## 7.5 Focus states

- All interactive elements require a visible keyboard focus indicator.
- Focus must not be only a color shift.
- Focus must not reuse the subtle `#24313D` structural rule as its sole boundary.
- The focus indicator must remain visible on dark and cyan surfaces.
- On dark surfaces, the default direction is a minimum `2px` cyan outline with separation from the control boundary.
- On cyan-filled controls, use a light outer outline with dark separation or an equivalently verified treatment.
- Forced-colors focus must use system colors and must not be suppressed.

---

# 8. Section-by-section selected specification

# 8.1 Hero

## Role

Establish the three-part business result, technical identity, and first useful action without delaying conversion.

## Desktop composition

- locked Header remains above the Homepage Hero;
- two-column composition;
- left: eyebrow, H1, supporting copy, primary and secondary actions, expectation copy, accountability line;
- right: supplementary system tableau;
- text column remains commercially dominant;
- tableau must not look like a product dashboard screenshot.

## Mobile composition

Order:

1. full RU eyebrow;
2. full RU H1;
3. full accepted supporting copy;
4. primary CTA;
5. expectation copy;
6. secondary CTA when retained by accepted Content Architecture;
7. accountability line;
8. simplified supplementary system signal when space permits.

The CTA must remain early enough to be useful. The full RU copy remains authoritative.

## System tableau

The tableau is semantic atmosphere for:

- trust;
- inquiry;
- response;
- follow-up.

It must be:

- static by default;
- supplementary;
- understandable if removed;
- implemented with lightweight HTML/CSS/SVG where later authorized;
- absent from accessibility reading order if purely decorative.

It must not imply a real analytics dashboard or unverified product platform.

## Prohibited Hero drift

- full-screen cinematic scene;
- auto-playing animation;
- massive neon glow;
- unreadable display typography;
- CTA below a decorative illustration on mobile;
- shortened RU copy without a separate approved correction;
- invented metrics or social proof.

---

# 8.2 Connected Business Journey

## Role

Show that the website and operational response process form one controlled system.

## Visual structure

- three friction fields;
- exactly four journey steps;
- one visible conclusion field;
- connected-line geometry may be used on desktop;
- mobile uses normal-flow stacked steps;
- no meaning depends on animation.

## Visual emphasis

The conclusion may receive the strongest border in the section. It must remain readable and not resemble a promotional banner.

## Short-landscape rule

At approximately `844 × 390`:

- no collision between the fourth step and conclusion;
- no horizontal poster dependency;
- section remains normal-flow and scrollable;
- the first viewport still prioritizes Hero action rather than Journey graphics.

---

# 8.3 Two Core Directions

## Role

Present only:

1. AI Systems & Automation;
2. Premium Websites & Branding.

## Desktop

Two equal-width direction fields are permitted.

## Mobile

Stack in accepted order without implying one is a cheaper or lower tier.

## Card content hierarchy

- direction title;
- problem/intervention/intended effect content;
- one service link.

## Prohibited drift

- third top-level service;
- feature-plan comparison;
- pricing-card treatment;
- popularity badge;
- icons that make the two directions look like generic SaaS features.

---

# 8.4 Financial Stream flagship proof

## Role

This is the strongest and earliest proof block on the Homepage.

## Desktop composition

- screenshot/proof field is the largest object;
- evidence panel is substantial but secondary;
- live-client status is visible before detailed copy;
- case-study and live-site actions remain visible;
- evidence and claim boundaries remain explicit.

Recommended desktop relationship:

```text
proof image field      approximately 58–62%
evidence panel         approximately 38–42%
```

## Mobile composition

Order:

1. section identity;
2. large screenshot/proof field;
3. exact live-client status at minimum `12px`;
4. heading and evidence copy;
5. evidence/claim-boundary field;
6. case and live-site actions.

## Screenshot treatment

- use real approved derivatives later;
- show enough interface context to be credible;
- avoid fake browser chrome that dominates the image;
- no floating glass cards over the screenshot;
- no invented charts, conversion figures, or ratings;
- crop must remain legible at mobile width.

## Required evidence discipline

The visual treatment must not imply:

- guaranteed rankings;
- revenue increase;
- conversion improvement;
- lead growth;
- measured ROI;
- performance metrics not supported by the case evidence.

## Distinctive weight

Financial Stream must feel visibly more important than each Selected Work project. It must not become one card among many equal product modules.

---

# 8.5 Ways to Start

## Role

Help a visitor identify the most relevant starting situation. This section is not pricing.

## Structure

Exactly three situations:

1. Website & trust;
2. Inquiry handling;
3. Connected system.

Each situation contains:

- title;
- “best when” content;
- first objective;
- what it does not imply;
- contextual Private Review action;
- deterministic service links.

Required service-link counts remain:

```text
Situation 1: 1
Situation 2: 1
Situation 3: 2
```

## Visual treatment

- editorial horizontal rows on desktop;
- full-width stacked fields on mobile;
- contextual review CTA visually separate from service links;
- no plan comparison columns;
- no price, feature count, recommendation badge, or “most popular” state.

---

# 8.6 Controlled Delivery

## Role

Reduce implementation risk by showing a controlled five-stage process.

## Stages

1. Review;
2. Define;
3. Build;
4. Verify;
5. Improve.

Localized labels remain governed by accepted Content Architecture.

## Desktop

A five-column sequence is permitted when each stage remains readable. At intermediate widths it may become `3 + 2`, a two-column flow, or a vertical sequence.

## Mobile

Stack stages in normal flow. Do not use a horizontal carousel for essential content.

## Visual style

The process should feel operational and accountable, not like generic agile methodology decoration.

---

# 8.7 Founder accountability

## Role

Show direct accountability without turning the Homepage into a personal-influencer page.

## Structure

- approved portrait field;
- concise accountability heading;
- bounded founder-led explanation;
- About and LinkedIn actions.

## Visual balance

- proof and process remain more important than portrait scale;
- portrait is professional and human;
- avoid dramatic founder hero photography;
- do not add unsupported credentials or authority claims.

## Responsive

At mobile, portrait and text may stack or use a compact media-object pattern when readability remains strong.

---

# 8.8 Selected Work

## Role

Show additional implementation range while preserving exact proof taxonomy.

Projects:

- Alina Horb;
- Local Repair Pro.

## Required project zones

Each project must have distinct normal-flow zones for:

1. project image;
2. project name;
3. exact status;
4. disclosure/evidence boundary;
5. project action.

## Proof taxonomy

### Alina Horb

Exact RU status:

```text
Действующий проект, связанный с основателем · UA/RU
```

The adjacent disclosure must state that it is founder-connected and not independent client validation.

### Local Repair Pro

Exact RU status:

```text
Концепция сайта · Рабочее демо · В разработке
```

The adjacent disclosure must state that it is not a paid client or operating repair company and is not evidence of real customer outcomes.

## Visual distinction

- labels, wording, borders, and disclosure placement must distinguish evidence classes;
- color alone is insufficient;
- both exact project statuses must remain at least `12px` on mobile;
- no tooltip, hover, accordion, or modal may hide the disclosure;
- no equal “client logo wall” treatment.

---

# 8.9 Insights

## Role

Provide useful pre-investment guidance while remaining subordinate to proof and conversion.

## Structure

- three selected articles;
- concise type labels;
- article title;
- article action;
- optional minimal metadata only when approved.

## Visual treatment

- cards may use the same precision system;
- avoid making Insights visually stronger than Financial Stream or Ways to Start;
- no decorative thumbnails are required;
- no autoplay or horizontal content carousel for essential access.

---

# 8.10 Final Private Review

## Role

Close with one clear, bounded next step.

## Visual structure

- section number and label;
- accepted final heading;
- primary Private Review CTA;
- concise boundary/expectation copy;
- locked Footer follows after the Homepage block.

## Prohibited drift

- new service argument;
- urgency countdown;
- fake scarcity;
- calendar-first replacement of structured request;
- repeated decorative system tableau;
- oversized glow that competes with the CTA.

---

# 9. Proof and disclosure system

## 9.1 Evidence classes

The Homepage must preserve three distinct classes:

1. real client project;
2. founder-connected project;
3. concept/demo in development.

## 9.2 Visual contract

Every proof item requires:

- explicit text status;
- visible disclosure when applicable;
- action separated from status;
- no implication based only on logo placement or image scale;
- no hidden caveat.

## 9.3 Status token

Status styling must use:

- minimum `12px` on mobile for every essential proof status or evidence classification;
- minimum `11px` on desktop;
- cyan text when compatible with the evidence system;
- concise uppercase only when readable at the required size;
- restrained letter spacing;
- short rule or boundary field where useful;
- normal-flow expansion for localized text.

Mobile long-form RU statuses may use title case, reduced letter spacing, and additional natural lines when necessary. Text fidelity and the mandatory `12px` minimum are more important than preserving uppercase styling or a one-line layout.

## 9.4 Disclosure token

Disclosure must use:

- minimum 14px on mobile;
- minimum 13px on desktop;
- line height at least 1.5;
- muted text with sufficient contrast;
- normal-flow placement directly adjacent to the status and before/near the project action.

---

# 10. Responsive behavior contract

## 10.1 Desktop 1440

Must demonstrate:

- full ten-block hierarchy;
- two-column Hero;
- visible but supplementary system tableau;
- Financial Stream as strongest proof;
- editorial Ways-to-Start rows;
- readable five-stage process;
- two-project Selected Work layout;
- final CTA before locked Footer;
- exact section-surface ownership across the full page.

## 10.2 Mobile 390

Must demonstrate:

- full RU Hero without clipping;
- early primary CTA;
- full expectation copy;
- normal-flow Connected Journey;
- strong Financial Stream screenshot and status;
- proof statuses at minimum `12px`;
- non-pricing Ways-to-Start stacking;
- exact project statuses and visible disclosures;
- no horizontal overflow;
- Concept A identity and assigned surface rhythm without desktop-only grid dependence.

## 10.3 Narrow mobile 320

Must demonstrate:

- natural H1 wrapping;
- no word break inside project statuses;
- all proof statuses at minimum `12px`;
- no clipped CTA label;
- minimum content gutter;
- readable disclosure type;
- no fixed-height content clipping;
- no forced micro-text to preserve desktop composition;
- preservation of the assigned section-surface rhythm.

## 10.4 Short landscape approximately 844 × 390

Must demonstrate:

- locked Header remains usable;
- primary Hero CTA remains reachable and visible without decorative obstruction;
- no viewport-filling Hero requirement;
- no section overlap;
- normal vertical scrolling;
- no horizontal poster layout;
- mobile/compact header behavior remains owned by the Header system;
- proof status minimums and interactive boundaries remain valid in compact mode.

---

# 11. Accessibility contract

The later implementation must satisfy:

- semantic heading order;
- keyboard access to every interactive element;
- visible focus indication;
- minimum touch targets;
- sufficient text and UI contrast;
- interactive boundaries with at least `3:1` non-text contrast where the boundary is required to identify the control;
- structural decorative rules kept separate from interactive and focus roles;
- no essential information encoded only through color;
- no essential text embedded in screenshots;
- descriptive image alternatives where images carry content;
- decorative system graphics hidden from assistive technology;
- `prefers-reduced-motion` support;
- forced-colors resilience;
- normal text selection;
- zoom and text reflow without clipping;
- skip-link and Header behavior preserved;
- no interaction that depends on hover or precise pointer movement.

The selected dark theme does not exempt the implementation from contrast verification. Muted text, structural rules, control boundaries, focus indicators, status labels, and disabled-like fields require explicit checks against their actual adjacent surfaces.

---

# 12. Motion contract

## 12.1 Default

No motion is required to implement Concept A successfully.

## 12.2 Potential later motion

Only after static implementation acceptance, a separate motion task may consider:

- restrained line reveal;
- small proof-image depth response;
- short hover/focus transition;
- one non-essential Hero system signal;
- reduced-motion equivalents.

## 12.3 Limits

```text
normal transition duration    approximately 160–240ms
large reveal maximum          approximately 320ms
continuous animation          prohibited by default
parallax                      prohibited by default
cursor-following effects      prohibited
```

No motion may delay reading, CTA access, or screenshot inspection.

---

# 13. Image and asset contract

This selected specification does not choose final assets.

Future asset work requires a separate reviewed task covering:

- Financial Stream screenshot derivatives;
- Alina Horb image crop;
- Local Repair Pro image crop;
- founder portrait treatment;
- responsive dimensions;
- WebP/AVIF/JPEG fallback where relevant;
- alt text ownership;
- focal points;
- file size budgets;
- social-preview assets only if separately authorized.

No decorative AI stock image is required.

Image fields should prove real work rather than create abstract futuristic atmosphere.

---

# 14. Current production architecture boundary

The current production handoff identifies Homepage wrappers and snapshot includes:

```text
index.html
ru/index.html
_includes/homepage-current-en.html
_includes/homepage-current-ru.html
```

This document does not decide whether Homepage V2 should:

- replace the current snapshot includes;
- introduce new versioned includes;
- use structured data/YAML;
- use a different safe composition strategy.

A later implementation-planning task must inspect the actual current `main`, determine the minimum safe architecture, and explicitly protect:

- Header system;
- Footer system;
- Contact integration;
- EN/RU route parity;
- metadata;
- canonical and hreflang;
- current scripts and analytics behavior;
- build/deploy verification.

No future Builder may copy an obsolete Homepage branch over current `main`.

---

# 15. Future implementation contract

After this corrected specification receives focused independent `ACCEPT`, the next authorized task is **implementation planning**, not automatic production coding.

That task must produce:

1. current-main SHA;
2. exact current Homepage architecture analysis;
3. exact proposed file scope;
4. data/content ownership plan;
5. Header/Footer/Contact preservation plan;
6. image derivative plan;
7. responsive test matrix;
8. accessibility and reduced-motion checks;
9. rollback strategy;
10. Builder/Reviewer branch plan;
11. explicit owner authorization gate for code.

## 15.1 Future implementation non-negotiables

- preserve accepted content and ten-block order;
- preserve the locked section-surface ownership map;
- preserve exact proof taxonomy;
- preserve all deterministic action/origin identifiers and Contact allowlists;
- preserve EN/RU localized routes and metadata;
- preserve mandatory mobile proof-status and disclosure minimums;
- separate structural, interactive-boundary, and focus roles;
- no invented metrics or testimonials;
- no unnecessary framework or dependency;
- no WebGL/canvas requirement;
- no Header/Footer redesign;
- no broad unrelated cleanup;
- no production PR before Builder checks and independent review.

## 15.2 Expected technical direction

The selected concept is compatible with:

- semantic HTML;
- CSS custom properties;
- responsive grid/flex layouts;
- minimal vanilla JavaScript only where necessary;
- lightweight inline or external SVG for supplementary system graphics;
- static/Jekyll production;
- progressive enhancement.

This is a direction, not a file-level authorization.

---

# 16. Production-quality acceptance criteria

A later implementation cannot be accepted unless all criteria below pass.

## Visual identity

- recognisably Concept A;
- dark graphite, white typography, restrained cyan signal;
- exact full-page section-surface ownership retained;
- no generic SaaS dashboard appearance;
- no crypto/startup gradient excess;
- no consulting-template loss of technical identity;
- grid and glow remain subordinate.

## Commercial hierarchy

- ten-block order exact;
- Hero action clear;
- Financial Stream strongest proof;
- Ways to Start after proof;
- Founder and Insights subordinate;
- final CTA introduces no new offer.

## Proof

- exact statuses;
- proof statuses at least `12px` on mobile;
- visible disclosures;
- no hidden caveats;
- no unsupported performance implication;
- Financial Stream clearly distinguished from Selected Work.

## Localization

- EN/RU semantic parity;
- natural localization;
- no clipping at 390 or 320;
- short-landscape viability;
- mapped links and metadata intact.

## Accessibility

- keyboard and focus pass;
- text contrast pass;
- non-text control-boundary contrast pass;
- structural rules not used as sole interactive boundaries;
- touch-target pass;
- reduced-motion pass;
- forced-colors reasonable;
- semantic structure pass;
- image alternatives pass.

## Production

- no unrelated files changed;
- Jekyll/build output verified;
- no console errors introduced;
- no horizontal overflow;
- Header, mobile menu, Contact, Footer, case links, and article links remain functional;
- cache-busting handled when existing shared assets change;
- rollback is clear.

---

# 17. Anti-drift checklist

Reject an implementation or correction that introduces any of the following without separate owner approval:

- extra top-level Homepage sections;
- a third service direction;
- pricing tiers;
- generic client-logo wall;
- invented reviews or metrics;
- continuous glow or animation;
- mint/violet Concept C palette;
- warm-light Concept B replacement of the selected dark system;
- fully pill-shaped cards/buttons throughout;
- hidden proof disclosures;
- mobile proof statuses below `12px`;
- disclosures below the specified minimums;
- `#24313D` used as the sole visible boundary for secondary buttons, controls, or focus;
- arbitrary or fully uniform section-surface assignment;
- fixed-height localized text regions;
- horizontal mobile carousels for essential content;
- new Header/Footer variants;
- Contact query values outside the accepted allowlist;
- direct production implementation before specification acceptance.

---

# 18. Decision gate

This corrected specification is ready for focused independent correction review.

The Reviewer must determine only:

1. whether the ten-block section-surface ownership map is complete, deterministic, and faithful to Concept A;
2. whether structural rules, interactive control boundaries, and focus-visible roles are separated with adequate non-text contrast requirements;
3. whether essential proof statuses have a mandatory `12px` mobile minimum while `11px` remains restricted to short section numbers and non-essential metadata;
4. whether these corrections avoid concept drift, content change, or premature implementation authorization;
5. whether the next stage may be implementation planning.

Allowed verdicts:

- `ACCEPT`;
- `TARGETED CORRECTION`;
- `REJECT`.

Until focused `ACCEPT`, do not begin implementation planning or Homepage production implementation.
