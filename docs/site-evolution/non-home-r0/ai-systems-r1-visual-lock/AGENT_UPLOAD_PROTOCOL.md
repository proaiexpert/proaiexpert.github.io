# Agent Upload Protocol

For every AI Systems R1 Visual Lock agent:

1. Read `docs/site-evolution/non-home-r0/AI_SYSTEMS_R1_GOLDEN_BLUEPRINT.md` at commit `536091991d05e8259ffdf9b5b7d1708bd36b3993`.
2. Use branch `agent/proai-ai-systems-r1-visual-lock` for review artifacts only.
3. Choose an unused folder under `docs/site-evolution/non-home-r0/ai-systems-r1-visual-lock/incoming/` such as `visual-b`, `visual-c`, etc.
4. Save generated concept frames directly into that folder. Do not ask the Owner to relay images manually.
5. Minimum visual package: Hero, Register, Pearl Decision, Human Check Gate, Mobile 390. A single board is acceptable only if each state is legible; separate frames are preferred.
6. Add `REVIEW.md` and `MANIFEST.md` in the same folder.
7. Do not touch production code, homepage files, other agents' folders, `shortlisted/`, or `approved/`.
8. Commit the review package and return the exact commit SHA + GitHub folder URL + short Russian owner summary.
9. If your environment cannot commit image binaries to GitHub, state `GITHUB VISUAL UPLOAD BLOCKED` before claiming completion. Do not fabricate links.

No merge. No deploy. No production build.
