# Incoming Visual Candidates

Raw independent Visual Lock candidates live here. Each agent uses its own subfolder and must not overwrite another agent's files. Nothing in `incoming/` is authoritative.
