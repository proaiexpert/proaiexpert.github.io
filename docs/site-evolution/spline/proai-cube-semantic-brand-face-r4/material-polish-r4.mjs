import fs from 'node:fs';
const file = new URL('./main.generated.js', import.meta.url);
let source = fs.readFileSync(file, 'utf8');
const one=(a,b,l)=>{const i=source.indexOf(a);if(i<0||source.indexOf(a,i+a.length)>=0)throw new Error(`R4.1 material anchor ${l}`);source=source.slice(0,i)+b+source.slice(i+a.length)};
const rx=(r,b,l)=>{const g=new RegExp(r.source,r.flags.includes('g')?r.flags:r.flags+'g');const m=[...source.matchAll(g)];if(m.length!==1)throw new Error(`R4.1 material regex ${l}: ${m.length}`);source=source.replace(r,b)};
one('  surfaceMaxOpacity: 0.065,','  surfaceMaxOpacity: 0.020,','surface opacity');
const material=`const SEMANTIC_R4_MATERIAL_PALETTE = Object.freeze({shadowMetal:'#6B7176',midSilver:'#959CA1',pearlSilver:'#C7CAC7',peakReflection:'#E0E1DD'});
function cutSemanticSeam(ctx,size,c,h,f,vertical){const a=c-h-f,b=c+h+f,g=vertical?ctx.createLinearGradient(a,0,b,0):ctx.createLinearGradient(0,a,0,b),t=f/Math.max(1,b-a);g.addColorStop(0,'rgba(0,0,0,0)');g.addColorStop(Math.min(.49,t),'rgba(0,0,0,1)');g.addColorStop(Math.max(.51,1-t),'rgba(0,0,0,1)');g.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g;vertical?ctx.fillRect(a,0,b-a,size):ctx.fillRect(0,a,size,b-a)}
function createSeamAwareBrandMaskTexture(texture,faceSpan){const canvas=texture.image,ctx=canvas.getContext('2d',{alpha:true}),size=canvas.width,stepY=Math.abs(latticeCenters.Y[1]-latticeCenters.Y[0]),stepZ=Math.abs(latticeCenters.Z[1]-latticeCenters.Z[0]),gapY=Math.max(0,stepY-GEOMETRY_R1.faceOuterSize),gapZ=Math.max(0,stepZ-GEOMETRY_R1.faceOuterSize),feather=Math.max(.65,Math.min(1.50,Math.max(gapY,gapZ)*.44));ctx.save();ctx.globalCompositeOperation='destination-out';for(const p of [(latticeCenters.Y[0]+latticeCenters.Y[1])*.5,(latticeCenters.Y[1]+latticeCenters.Y[2])*.5])cutSemanticSeam(ctx,size,(.5+p/faceSpan)*size,gapY*.5/faceSpan*size,feather/faceSpan*size,true);for(const p of [(latticeCenters.Z[0]+latticeCenters.Z[1])*.5,(latticeCenters.Z[1]+latticeCenters.Z[2])*.5])cutSemanticSeam(ctx,size,(.5-p/faceSpan)*size,gapZ*.5/faceSpan*size,feather/faceSpan*size,false);ctx.restore();texture.needsUpdate=true;texture.userData.semanticSeamMask={gapY,gapZ,feather,source:'latticeCenters + GEOMETRY_R1.faceOuterSize'};return texture}
function createSemanticTextMaterial(maskTexture){const peakReflection=SEMANTIC_R4_MATERIAL_PALETTE.peakReflection;const m=new THREE.MeshPhysicalMaterial({color:SEMANTIC_R4_MATERIAL_PALETTE.shadowMetal,map:maskTexture,metalness:.68,roughness:.46,clearcoat:.04,clearcoatRoughness:.34,envMapIntensity:.78,ior:1.48,specularIntensity:.62,specularColor:new THREE.Color(peakReflection),transparent:true,opacity:0,alphaTest:.006,depthWrite:false,depthTest:true});m.toneMapped=true;m.userData.semanticMaterial='SEMANTIC_R4_1_SOFT_OPTICAL_PEARL_SILVER_INLAY';return m}`;
rx(/function createSemanticTextMaterial\(maskTexture\) \{[\s\S]*?\n\}\n\nfunction setupSemanticSurface/,`${material}\n\nfunction setupSemanticSurface`,'physical material');
one('  semanticMaskTexture = createBrandMaskTexture();','  semanticMaskTexture = createSeamAwareBrandMaskTexture(createBrandMaskTexture(), faceSpan);','seam mask');
const visual=`function setSemanticVisualState({ face = semanticFace, surface = 0, formation = 0, luminance = 0, sweep = 0.50, exit = 0 } = {}) {
  if (!semanticReady) return false;
  orientSemanticGroup(face);
  semanticSurfaceProgress=THREE.MathUtils.clamp(surface,0,1);semanticTextFormation=THREE.MathUtils.clamp(formation,0,1);semanticTextLuminance=THREE.MathUtils.clamp(luminance,0,1);semanticSweep=.50;
  const release=THREE.MathUtils.clamp(exit,0,1),rawPresence=semanticTextFormation,emergence=smootherstep(THREE.MathUtils.clamp((rawPresence-.06)/.94,0,1)),baseLift=smootherstep(THREE.MathUtils.clamp((emergence-.16)/.84,0,1)),glintIn=smootherstep(THREE.MathUtils.clamp((semanticTextLuminance-.30)/.70,0,1)),glintOut=1-smootherstep(THREE.MathUtils.clamp((release-.03)/.74,0,1)),catchStrength=glintIn*glintOut,shadow=new THREE.Color(SEMANTIC_R4_MATERIAL_PALETTE.shadowMetal),mid=new THREE.Color(SEMANTIC_R4_MATERIAL_PALETTE.midSilver),pearl=new THREE.Color(SEMANTIC_R4_MATERIAL_PALETTE.pearlSilver);
  semanticSurface.material.opacity=SEMANTIC_R4.surfaceMaxOpacity*semanticSurfaceProgress*.38;semanticSurface.material.roughness=THREE.MathUtils.lerp(.405,.395,semanticSurfaceProgress);semanticSurface.material.envMapIntensity=THREE.MathUtils.lerp(.90,.94,semanticSurfaceProgress);
  semanticTextMeshes.forEach((mesh,index)=>{const soft=index===0,m=mesh.material,c=shadow.clone().lerp(mid,soft?.24+baseLift*.18:.34+baseLift*.28);c.lerp(pearl,catchStrength*(soft?.10:.28));m.color.copy(c);m.opacity=emergence*(soft?.095:.64);m.metalness=soft?.60:.72;m.roughness=THREE.MathUtils.lerp(soft?.54:.46,soft?.45:.34,catchStrength);m.clearcoat=THREE.MathUtils.lerp(soft?.02:.04,soft?.05:.08,catchStrength);m.clearcoatRoughness=THREE.MathUtils.lerp(.36,soft?.32:.27,catchStrength);m.envMapIntensity=THREE.MathUtils.lerp(soft?.68:.82,soft?.86:1.12,catchStrength);m.specularIntensity=THREE.MathUtils.lerp(soft?.48:.58,soft?.64:.82,catchStrength)});
  semanticGroup.visible=semanticSurfaceProgress>.001||emergence>.001;return true;
}`;
rx(/function setSemanticVisualState\([\s\S]*?\n\}\n\nfunction setSemanticReviewState/,`${visual}\n\nfunction setSemanticReviewState`,'visual state');
const clear=`function clearSemanticReviewState(){if(!semanticReady)return false;semanticSurfaceProgress=0;semanticTextFormation=0;semanticTextLuminance=0;semanticSweep=.50;semanticSurface.material.opacity=0;semanticTextMeshes.forEach((mesh,index)=>{const soft=index===0,m=mesh.material;m.opacity=0;m.color.set(SEMANTIC_R4_MATERIAL_PALETTE.shadowMetal);m.metalness=soft?.60:.72;m.roughness=soft?.54:.46;m.clearcoat=soft?.02:.04;m.clearcoatRoughness=.36;m.envMapIntensity=soft?.68:.82;m.specularIntensity=soft?.48:.58});semanticGroup.visible=false;return true;}`;
rx(/function clearSemanticReviewState\(\) \{[\s\S]*?\n\}\n\nfunction semanticTimelineState/,`${clear}\n\nfunction semanticTimelineState`,'clear state');
one('    textMeshCount: semanticTextMeshes.length,\n',`    textMeshCount: semanticTextMeshes.length,\n    materialModel: 'MeshPhysicalMaterial / R4.1 soft optical Pearl-Silver inlay',\n    materialPalette: SEMANTIC_R4_MATERIAL_PALETTE,\n    revealCharacter: 'soft subsurface emergence / reflection-first exit',\n    seamAwareOcclusion: true,\n    seamMaskSource: semanticMaskTexture?.userData?.semanticSeamMask?.source || null,\n`,'diagnostics');
fs.writeFileSync(file,source);
console.log(JSON.stringify({material:'MeshPhysicalMaterial / R4.1 soft optical Pearl-Silver inlay',surfaceMaxOpacity:.020,palette:{shadowMetal:'#6B7176',midSilver:'#959CA1',pearlSilver:'#C7CAC7',peakReflection:'#E0E1DD'},revealCharacter:'soft subsurface emergence / reflection-first exit',seamAwareOcclusion:true,microHalo:false,uvSweep:false,selfLitShader:false,timingChanged:false,motionChanged:false},null,2));
